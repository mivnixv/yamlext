[package]
name = "yamlext"
version = "0.1.0"
edition = "2024"
description = "YAML processor with !include and !merge custom tags"

[[bin]]
name = "yamlext"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde_yaml = "0.9"
anyhow = "1"
regex = "1"
